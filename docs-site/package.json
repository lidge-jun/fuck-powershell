{
  "name": "fuck-powershell-docs",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "graph": "bun ../scripts/build-graph.mjs && bun ../scripts/validate-graph.mjs && bun ../scripts/lint-cases.mjs",
    "sync": "bun ../scripts/sync-cases.mjs && bun ../scripts/sync-ontology.mjs",
    "dev": "bun run graph && bun run sync && astro dev",
    "build": "bun run graph && bun run sync && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.7",
    "astro": "^7.2.2",
    "sharp": "^0.35.3"
  }
}
