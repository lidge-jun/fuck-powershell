# 001 — Concept synthesis (element ledger)

Both concepts inspected via view_image. Ledger:

| Token | Source | Decision |
|---|---|---|
| At-a-glance card | case-anatomy | ADOPT: solid #161618 rounded-16, hairline rows, gray label column (Affects / Fails as / Mechanism / Safe fix), chips reuse existing badge styles; green fix line WITHOUT icon glyph (no new icon set — text + color is enough) |
| Eyebrow label | case-anatomy | ADOPT: "CATEGORY · CASE" uppercase gray eyebrow above h1 (generated by sync-cases) |
| Code block quieting | case-anatomy | ADOPT: flatter frame, hairline border (already 16px radius) |
| Category cards | cases-index | ADOPT: 2-col grid, colored rounded-square glyph (reuse dock SVG tiles!), name+count+description, case rows with chevrons. Real case titles (concept's were lorem) — use short navLabels |
| Masonry height | cases-index | ADOPT: natural height per content, no equal-height forcing |
| Blue link rows | cases-index | MODIFY: quiet text color at rest, blue on hover (Apple lists are quieter than concept) |

Steering note applied: typography-rhythm + noise-reduction work (000 audit items
1-6) is the PRIMARY track and precedes the card work in implementation order.
