# 040 — Ship (mw5)

bun scripts/lint-cases.mjs (all ~25 cases), scripts/build-skill.mjs regen,
docs-site build exit 0, commit, push, gh run success, live 200 + spot-check one
new case page. Update README case count if hardcoded (check).
