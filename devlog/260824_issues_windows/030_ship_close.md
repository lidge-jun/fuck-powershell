# 030 — Ship + close (iw4)

1. bun scripts/build-skill.mjs regen; SKILL.md reference table update.
2. docs-site build exit 0; commit; push; gh run watch success; live 200 spot-check.
3. For each registered issue: gh issue comment with case path + live URL, then
   gh issue close. Any rejected issue gets a reasoned comment, stays open only
   if user action needed (none expected — all 11 are NEW per 000 plan).
4. Close criteria with receipts.
