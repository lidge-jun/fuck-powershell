# 050 — Ship (ow6)

Full pipeline: docs-site build chain (build-graph && validate-graph && lint &&
sync && sync-ontology && astro build) + build-skill regen → commit(s) → push →
deploy green (CI now runs the validator inside bun run build) → live 200 on
/ontology/ + one case page. README: widened Windows scope + ontology + fp
lookup section. Close criteria with receipts.
