---
id: workaround-functional-interpreter-probe
type: Workaround
label: "functional interpreter probe"
---

## Definition

Accept an interpreter only when it demonstrably does the job: executes a trivial program, or proves it can see the file it is about to run. Keep the absolute path that passed and reuse it.

