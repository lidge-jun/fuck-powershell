---
id: workaround-dispatch-by-declared-interpreter
type: Workaround
label: "dispatch by declared interpreter"
---

## Definition

Run a script with the interpreter it declares, read from its shebang or recorded in a descriptor beside it, rather than the one its filename or its caller implies.

