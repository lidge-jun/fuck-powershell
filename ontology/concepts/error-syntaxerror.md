---
id: error-syntaxerror
type: ErrorSignature
label: "syntaxerror"
---

## Definition

An interpreter reports a syntax error in a file that is valid in the language it was actually written in, because something else chose which interpreter would run it.

