---
id: workaround-call-operator
type: Workaround
label: "call operator"
---

## Definition

Invoke a quoted path with the & call operator so PowerShell parses the line in command mode rather than expression mode, keeping the quotes that a path with spaces needs.

