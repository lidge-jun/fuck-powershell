---
id: command-taskkill
type: Command
label: "taskkill"
---

## Definition

The Windows command that terminates processes by PID or image name, whose /T flag walks the live parent-PID chain rather than an opted-into group.
