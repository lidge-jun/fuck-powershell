---
id: workaround-structured-output-not-text
type: Workaround
label: "read exit codes and structured output, not prose"
---

## Definition

Prefer an exit code, a typed PowerShell object, or XML and JSON element names over matching translated console text, and probe the resource itself rather than parsing a tool's rendered opinion of it.
