---
id: workaround-ampersand-separator
type: Workaround
label: "join inline cmd commands with ampersand"
---

## Definition

Separate commands passed to cmd /c with an ampersand rather than a newline, keeping each redirection attached to its own command, or write a batch file when the script is long enough that newline semantics matter.
