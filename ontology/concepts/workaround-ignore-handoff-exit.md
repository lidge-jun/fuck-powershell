---
id: workaround-ignore-handoff-exit
type: Workaround
label: "ignore handoff exit"
---

## Definition

Treat Windows shell-handoff binaries (explorer, start) as fire-and-forget; never encode success as exit 0.
