---
id: workaround-per-dispatcher-command
type: Workaround
label: "per dispatcher command"
---

## Definition

Emit a different command string for each dispatcher a host declares, instead of one line assumed to be valid under every shell. A cmd /c prefix that rescues PowerShell is destroyed by a Git Bash hop, so the choice belongs next to the consumer.

