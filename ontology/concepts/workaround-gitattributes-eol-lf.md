---
id: workaround-gitattributes-eol-lf
type: Workaround
label: "gitattributes eol lf"
---

## Definition

Pin line endings in .gitattributes instead of relying on each contributor's core.autocrlf, with explicit entries for files that must not be normalized, such as a polyglot .cmd that cmd.exe dispatches.

