---
id: workaround-forward-slashes-in-config
type: Workaround
label: "forward slashes in config"
---

## Definition

Write drive-letter paths with forward slashes in configuration. The loaders that consume these configs accept them and no escape layer consumes them. This is advice about config strings, not about every Windows consumer.

